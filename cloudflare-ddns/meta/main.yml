---
galaxy_info:
  author: Stiven Stepanovic
  description: "A role to deploy a docker container that updates your Cloudflare A-Record with your public IP-Address every 5 minutes"
  license: license (GPLv2, CC-BY, etc)
  min_ansible_version: "1.2"
  platforms:
  - name: Docker
  #  versions:
  #  - all
  #  - 5
  #  - 6

dependencies: []
  # List your role dependencies here, one per line. Only
  # dependencies available via galaxy should be listed here.
  # Be sure to remove the '[]' above if you add dependencies
  # to this list.
  
